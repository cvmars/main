define(function(require) {

    var core = require('./interface');
    var utils = require('../utils');
        utils.FN.init();
    //require("WdatePicker");
    //utils.FN.mask(0);
    core.init();


});
