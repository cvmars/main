/*
 *  @name 购物车列表页面UI交互
 *  @Copyright feiniu         //版权声明
 *  @Designed @joker.ye       //制作人
 *  @date 2014-12-29           //制作时间
 *  @update 2014-12-29        //最后更新时间
 */
define(function(require, exports, module) {

    // 引入jquery
    var $ = require('jquery');

    //引入underscore
    var underscore = require('lib/underscore/1.7.0/underscore.js');

    // _.each([1,2,3], alert);  //alert 1,2,3
    // _.each({one: 1, two: 2, three: 3}, alert);   //alert 1,2,3
    var map = _.map([1, 2, 3], function(num){ return num * 3; });




});
